# ── Core scraping ──────────────────────────────────────────────────────────
httpx[http2]>=0.27.0          # Async HTTP client with HTTP/2 support
beautifulsoup4>=4.12.0
lxml>=5.0.0

playwright

# ── Config ─────────────────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0      # BaseSettings + .env file support


# ── Storage backends (optional) ────────────────────────────────────────────
# redis>=5.0.0                # Uncomment for Redis storage / distributed mode
# psycopg2-binary>=2.9.9      # Uncomment for PostgreSQL exporter

# ── Testing ────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# ── Dev / observability (optional) ────────────────────────────────────────
# prometheus-client>=0.20.0   # Uncomment for real Prometheus metrics


pandas